# SENTINEL - MOCK BACKEND REQUIREMENTS
# Kumud's scope ONLY: FastAPI + Faker mock data
# ML/DL packages (PyTorch, XGBoost, etc.) handled by teammate

# Core Backend
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database (SQLite for dev)
aiosqlite==0.19.0

# Mock Data Generator
faker==22.0.0
